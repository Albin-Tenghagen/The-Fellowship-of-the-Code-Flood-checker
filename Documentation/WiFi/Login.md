When attempting to create the `sendLoginRequest` function I encountered a core panic and began to search for possible errors. First I assumed it happened because of the `JsonDocument` object because it occured when I used it but I soon discovered that if I put any code in the function it broke. However, after some testing I arrived at a conclusion. I forgot to return a boolean at the end of the function which crasched the MCU completely.